from setuptools import find_packages, setup
# from typing import List

# def get_requirements()->List[str]:
#     """
#     This will return the list of the requirements
#     """
#     req_list:List[str] = []
#     # open requiremnts.txt and append in this list
#     return req_list

setup(
    name='sensor',
    version='0.0.1',
    author='Deepak Porwal',
    author_email='dporwal04@gmail.com',
    packages= find_packages(),
    # install_requires=[],
)